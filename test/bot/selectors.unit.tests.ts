describe('state selectors unit tests', () => {
  describe('getProduct', () => {
    it(`should find a product in state by exchange, product base currency,
        and product quote currency`, () => {
      
    })
  })
})